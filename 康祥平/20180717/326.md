```java
class Solution {
    public boolean isPowerOfThree(int n) {
           
         if((int) Math.log(n) / Math.log(3)== Math.log(n) / Math.log(3))
             return true;
        else
            return false;
    }
}
```

